# Example .diringest configuration file (TOML format)
# Place this file as `.diringest` in your project's root directory.
# Settings here are overridden by command-line arguments.

# Default output format ('markdown' or 'json')
# format = "json"

# Default output file (if not specified, defaults to clipboard/stdout)
# output = "project_digest.md"

# Maximum file size in Kilobytes to include content for
# max_size = 500 # Increase from default 300KB

# Disable default exclusion patterns (equivalent to --no-default-ignore)
# default_ignore = false

# Explicitly disable clipboard copy even if --output is not used
# clipboard = false

# Add custom exclusion patterns (these supplement defaults unless default_ignore=false)
# exclude = [
#   "docs/",
#   "*.test.js",
#   "data/"
# ]

# Explicitly include only specific patterns (use with caution, excludes everything else)
# include = [
#   "src/**/*.py",
#   "README.md",
#   "pyproject.toml"
# ]

# Enable verbose logging by default (equivalent to -v)
# verbose = true

# --- Advanced / Internal Options ---
# Change default encoding (use with care)
# encoding = "latin-1"

# Follow symbolic links (default is false, use with caution)
# follow_symlinks = true